// Write a function that checks if a given word or phrase is a palindrome.

// Example:
// Input: "racecar"
// Output: true

// "aba" is palindrome because the backword of "aba" is still "aba"

function isPalindrome(str) {}

module.exports = isPalindrome;
